# 复制为 .env 并按需修改

# 运行环境
ENV=development
# 日志级别
LOG_LEVEL=INFO

# 数据库连接(SQLite 默认)
DATABASE_URL=sqlite:///./data/web.db

# API 监听
HOST=0.0.0.0
PORT=8000

# 调度开关(是否启用定时任务)
SCHEDULER_ENABLED=false

# 集思录登录(可转债/等权指数数据源, 必填)
JISILU_USERNAME=
JISILU_PASSWORD=
