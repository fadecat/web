# 组合实验室 · 文档索引（唯一入口）

- 日期：2026-09-19
- 状态：**算法与页面规格已定稿并验证通过；P0（股票/ETF）+ P1（场外基金）已实现并端到端验收，P2 未开始**
- 一句话：复刻韭圈儿（funddb.cn）组合详情页的"组合收益 + 组合详情 + 相关性"主干，**增强点 = 标的层级下探到单一股票**（支持 股票 / ETF / 场外基金 三类混搭）

> ⚠ **两套"P0/P1"编号并存，读代码前务必区分**（曾造成混淆）：
>
> | 编号 | 含义 |
> | --- | --- |
> | **提交里的 P0-1 / P0-2 / P0-3** | 是**文档 P0 阶段内部的三次提交**：`280a85b` 基础层 → `bab81fe` 服务+API → `9e16f92` 前端 |
> | **文档里的 P0 / P1 / P2 / P3** | 是**四个阶段**（见 `flow.md` §七 + `data-maintenance.md` §十） |
>
> 文档分期与落地状态：**P0** 股票/ETF 注册与按需抓取 ✅（`280a85b`/`bab81fe`/`9e16f92`）｜**P1** 场外基金链路 ✅（`6fd2350`）｜**P2** 统一序列层 + 组合定义 ⬜｜**P3** 回测引擎 + 展示 ⬜

> 本文是入口。先读这里，再按需进入各专题文档。

---

## 一、文档地图

**10 份专题 + 3 个可执行工具**。

| # | 文档 | 作用 | 何时读 |
| --- | --- | --- | --- |
| 0 | **`portfolio-lab-ambiguity-audit.md`** | **歧义审计**：明确 28 / 仍有歧义 7（均为产品取舍）/ 待核实 1；**红线项已全部关闭** | 开工前第一份读它 |
| 1 | **`portfolio-lab-verification.md`** | **验收报告**：三方核对清单（本版数据 / 韭圈儿 / 页面应显示）、六类 24 点实测结果、口径定稿、残差分析、验收断言 | **开工前必读**；实现后用它做验收 |
| 2 | **`portfolio-lab-page-spec.md`** | **页面规格**：§一~八 = **组合详情页（L2，8 个区域）**；§九 = **组合列表页（L1，按实拍截图还原）**；含视觉规范、数值来源映射、接口契约 | 做前端 + 定接口时读 |
| 3 | **`funddb-portfolio-algorithm.md`** | **算法原理**：破解过程、口径推导、6 步定义、5 个陷阱 | 怀疑口径或要改算法时读 |
| 4 | `portfolio-lab-flow.md` | **端到端结构与流程**：五段流程、数据结构（复用/新增表）、统一序列层、**端点全表（含 P0~P3 分期）** | 做后端与建表时读（⚠ **§五 已作废**） |
| 5 | `portfolio-lab-multi-portfolio.md` | **为什么是多组合 + 建模归属**：〇之二（用户裁决 + 更正记录 0.2.5）、列表页设计、增量量化、5 条风险 | 定表结构前读 |
| 6 | **`portfolio-lab-add-asset-ux.md`** | **组合内怎么加标的**：入口两态、5 步流程、多候选、组合起点变化提示、8 种异常 | **动 P0 前读**（P0 就是做这段） |
| 7 | `portfolio-asset-universe-feasibility.md` | **标的接入可行性**：三类资产数据可得性、`research_security` 现状、按需抓取设计、4 个决策点 | 做"添加标的"时读 |
| 10 | 🔴 **`portfolio-lab-arch-review.md`** | **架构与产品 Review**：🔴 ETF 口径错误（验收用蛋卷净值、生产设计用腾讯场内价，差 **+88pp**）+ 可砍复杂度（两态/异步/行话）+ 5 处代码层建议 | **开工前必读** |
| 9 | **`portfolio-lab-data-maintenance.md`** | **数据维护设计**：三类标的的获取/定时/手动/幂等/修订/新鲜度；复用既有骨架 + 三处结构性障碍 + 两个 job + 三种触发 + 4 项待拍板 | **动 P0/P1 前读** |
| 8 | `portfolio-backtest-engine-design.md` | 早期引擎设计回复。**文首有 ⚠ 更新块、正文有 ❌ 内联标记**；仅"数据现状 / 表复用 / 交易日历 / Run 范式 / 异步隔离与 NAV 存储"仍有效 | 查历史决策时读 |
| — | `scripts/verify_against_funddb.py` | **回归工具**：`--only win\|rebal\|corr\|weight\|added\|day`，当前 **6 类全部 PASS** | 每次改引擎后跑一次 |
| — | `scripts/measure_exec_lag.py` | **成交时点敏感性工具**：当日净值 vs T+1 的收益/回撤差（D2 的量化依据） | 调整成交口径时跑一次 |
| — | `scripts/probe_danjuan.py` | 蛋卷接口结构验证（克制版，>5 标的拒绝执行） | 改蛋卷抓取时跑一次 |

---

## 二、推荐阅读顺序

```
① 本文（索引）                    ← 口径速查 + 待办清单都在这里
② portfolio-lab-ambiguity-audit.md  ← 明确/歧义/待核实三分类，红线项已全关闭
③ portfolio-lab-verification.md     ← 先看"结论先行"与"§五 验收断言"
④ portfolio-lab-page-spec.md        ← 要做成什么样（§一~八 = 详情页；§九 = 列表页）
⑤ portfolio-lab-multi-portfolio.md  ← 为什么是多组合、平衡方式为何归 Run
⑥ portfolio-lab-flow.md             ← 分几段、动哪些表、走哪些接口
⑦ portfolio-lab-add-asset-ux.md     ← 组合内怎么加标的（P0 就是做这段）
⑧ funddb-portfolio-algorithm.md     ← 需要抠口径时再进
```

---

## 三、已定稿口径速查

| # | 口径 | 值 |
| --- | --- | --- |
| 1 | 建仓日 T0 | **2013-04-26** = 各标的**数据可得区间**的共同起点。**不引入"成立日/上市日"外部元数据**（用户 2026-09-19 裁决：回测起止日 = 数据起止日；个股无此接口） |
| 2 | 「成立来」起算 | 自 T0 起算。韭圈儿自成立日面值起算 → 差 **1.27pp**（口径差异，非误差），该格容差 ±1.5pp |
| 3 | 起点规则 | **共同**：`T_start = max(用户选择, T0)`，更早日期**自动前移** |
| 4 | 复权口径 | **分红再投日增长率链式**（蛋卷 `percentage`），禁用单位净值 |
| 5 | 账本 | **份额法**：`n_i = w_i / P_i(T0)`，`NAV(t) = Σ n_i·P_i(t)` |
| 6 | 区间查询 | 取净值序列两点比值，**不重置权重** |
| 7 | 再平衡 | 只支持 **不平衡 / 季平衡 / 年平衡**（韭圈儿无月/阈值选项） |
| 8 | 交易成本 | **不计** |
| 9 | 交易日口径 | **并集 + 前值填充** |
| 10 | 近1日 | `Σ wᵢ × rᵢ(各自最新可用日)`（QDII 前值补偿） |
| 11 | 相关性 | 日涨跌幅皮尔森，交集对齐，**计算区间独立于回测区间** |
| 12 | 完整曲线模式 | **不做**（起点规则互斥 + "空仓"语义未验证） |
| 13 | **再平衡/基准/区间的归属** | **属于 `backtest_run`（参数即身份），不属于 `portfolio`**。组合只描述"持有什么、各多少"。`portfolio` 可存 `default_*` 仅作预填 |
| 14 | **组合范围** | **支持多组合**（用户可创建多个）。**L1 组合列表**（管组合：新建/进入/复制/改名/删除）+ **L2 组合详情**（复刻韭圈儿，管标的与回测）。⚠ "多组合 → 单组合"指的是**页面层级**（列表 → 详情），不是数量收敛 |
| 15 | 组合内标的重复 | **禁止**（唯一键 `(portfolio_id, symbol)`） |
| 16 | `added_at` 位置 | **成员表**（同一标的在不同组合添加日不同） |

---

## 四、待办清单（**本节为权威**，其他文档的待办均汇总至此）

### 4.1 红线项：✅ 已全部关闭（无需再决策）

| 原编号 | 裁决 |
| --- | --- |
| **D1 / V1** 起算日 | 不找"成立日/上市日"，一律以**数据可得区间**为准（T0） |
| **D2** 场外成交时点 | **当日净值成交**（T+1 会让收益偏 0.6~2.8pp） |
| **D4** 添加日来源 | 纯展示列，**对回测零影响** → 非阻塞 |
| **V2** 个股行的不确定列 | 留占位符 `—` |
| **V4 / D7** 对比基准 | **做成可选**（可跟红利 / 任意 ETF / 个股对照） |

> ✅ **新增 1 条已裁决红线（用户 2026-09-19）**：
>
> | # | 事项 | 裁决 |
> | --- | --- | --- |
> | **E1** | **ETF 的数据口径**：腾讯场内价 vs 蛋卷净值，513100 近10年差 +88.30pp | **不是 bug，是产品差异**。场内/场外溢价、不同场外基金收益差异均可接受。**两种口径都支持**（`HFQ_PRICE` + `NAV_ADJ`），**用户自选具体产品**——想对齐韭圈儿净值口径就加场外基金，想跟踪场内成交就加场内 ETF。`data-maintenance.md` 原始拆分成立（个股/ETF → 腾讯，场外 → 蛋卷） |

### 4.2 曾待拍板（**10 项已全部落地**，2026-09-19 复核）

> ✅ **①~⑥ 已在 P2/P3 落地**：`added_at` 放成员表 · 唯一键 `(portfolio_id, symbol)` 禁止同组合重复 · 详情页**不加**组合切换下拉 · 编辑态/查看态两态切换 · 权重在添加后统一设 · 北交所不纳入（`normalize_code` 前缀表）。
> ✅ **⑦~⑩ 已在 P0/P1 落地**：`{code}.OF` 标识 · `history_start` → 2013-01-01 · `fund_nav_sync` 23:10 · `catalog()` source 读配置。

| # | 事项 | 建议 | 出处 |
| --- | --- | --- | --- |
| ① | `added_at` 放**成员表**还是标的主表 | **成员表**（同一标的在不同组合添加日不同） | multi-portfolio §七-② |
| ② | 组合内同一标的**是否允许重复** | **禁止**（跨组合允许重复） | multi-portfolio §七-④ |
| ③ | 详情页是否再加"**组合切换下拉**" | **不加**（L1 列表已够用） | multi-portfolio §七-⑧ |
| ④ | **添加标的的入口形态**：查看态/编辑态切换，还是单页内联 | **两态切换** | add-asset-ux §六-① |
| ⑤ | **权重填写时机**：添加时填 vs 添加后统一设 | **添加后统一设** | add-asset-ux §六-④ |
| ⑥ | **北交所股票**（`4/8` 开头）是否纳入 | **不纳入**（腾讯可用性未验证） | add-asset-ux §六-⑤ |

> 其余 add-asset-ux §六-②（类型 chip）、③（名称搜索先按代码）、⑥（起点前移+提示）三条**已写进成品文档**，视为已采纳，无需另议。

**数据维护侧新增 4 项**（出处：`portfolio-lab-data-maintenance.md` §十一）—— **P0/P1 已按"建议"列落地**，逐项落地情况见该文档 §十一：

| # | 事项 | 建议 |
| --- | --- | --- |
| ⑦ | **场外基金的标识形态** | `100018.OF`（显式，避免与股票/指数代码混淆） |
| ⑧ | **`history_start` 放宽到哪一年** | **2013-01-01**（当前 `2021-01-01` → **基准组合 T0=2013-04-26 跑不了**） |
| ⑨ | **`fund_nav_sync` 调度时刻** | **23:10**（给 QDII 净值公布留时间） |
| ⑩ | **`catalog()` 的 source 硬编码 bug** | **顺手修**（现写死 `akshare`，实际源是 `tencent` → 数据管理页来源显示错误） |

### 4.3 实现前需定、但可边做边定的技术项

| # | 事项 | 现状 | 何时要定 |
| --- | --- | --- | --- |
| 1 | **序列层取数起点** | ✅ 已定：由 T0 决定，可能早至 2003。**不得依赖 `research_daily_bar_adjusted`**（`history_start: "2021-01-01"` + 仅 5 标的 → 2013 复刻不可行）。股票/ETF 走腾讯、场外走蛋卷，落独立价格表 | 已定 |
| 2 | `research_security` 扩 **`FUND`** 类型 | ✅ **已完成**。`_assert_symbol_type` 已加 FUND 独立分支（`market_data.py:294`，只校验六位纯数字、不查交易所后缀）；标识形态 `{code}.OF` | P0/P1 |
| 3 | `fund_nav_daily` 的字段与主键 | ✅ **已建**（迁移 **0006**，⚠ 原写 0005 已被 P0-1 占用）。`unit_nav` + `daily_return_pct` 为事实、`adj_nav` 为链式派生值，唯一键 `(symbol, nav_date)` 幂等覆盖 | P1 迁移 0006 |
| 4 | **Benchmark 口径**（价格指数 vs 全收益） | ⬜ 未定。**不阻塞本模块回测**，只影响"相对基准超额"一列。⚠ 已证实韭圈儿用**价格指数**（沪深300 39.17% 精确复现） | P3 前 |
| 5 | 口径可比性：ETF `HFQ` 后复权 vs 基金**分红再投** | ⬜ 建议做一次小验证（两者同为"分红再投"语义，理论上可比） | P2 |
| 6 | 指标卡要不要（韭圈儿无此区） | 建议要（折叠默认收起） | P3 |
| 7 | 相关性区间在"标的不全同期"时的口径 | 建议 pairwise 交集 + 显示样本数 n | P3 |
| 8 | 退市/清盘标的的处理 | 建议 v1 不支持 | P2 |
| 9 | Run 幂等（同参数复用不新建） | 建议做（Run 数 = 组合 × 平衡 × 区间，会膨胀） | P3 |
| 10 | 权重约束（必须和 = 100%） | 建议硬校验，无现金腿 | P2 |
| 11 | **跨组合对比**（不同持仓并排） | 二期。⚠ 须"对齐交集区间"（各组合共同起点不同） | 二期 |

> ✅ **P3 复核（2026-09-19，实现完成）**：#4 基准口径**已实现**（指数走 `index_daily_quote` 并标 `PRICE`；返回体带 `price_basis_note`，前端区域⑧显式提示"价格指数不含股息、不可直接比"）· #6 指标卡**已做**（区域⑤，8 格）· #7 **已做** pairwise 交集 + `sample_sizes` · #9 Run 幂等**已做**（`input_hash` = 组合快照 + 参数摘要）· #10 权重硬校验**已做**（缺项 / 合计≠100% / 非正权重一律拒绝）。
> #5 由 E1 裁决为**产品差异**；#8 退市/清盘标 v1 不支持（维持）；**#11 跨组合对照仍是二期**。

### 4.4 实现复核：按时间顺序通读全部文档后的差距（2026-09-19 21:20）

> 起因：用户实测后提出"**交互比最初设计少了很多**"。于是按文档**修改时间顺序**逐份核对
> （`verification` → `page-spec` → `ambiguity-audit` → `multi-portfolio` → `flow` → `add-asset-ux`），
> 把"设计已写、实现没做"的项集中列在本节。**与 §4.1~4.3 并列，是待办的第二来源。**

| 出处（按时间） | 设计要求 | 状态 |
| --- | --- | --- |
| `flow.md` §五（唯一仍有效的一条） | 导航须为**一级模块「组合」** | ✅ 已补（原先错放进「研究」组） |
| `page-spec` §四 | 未添加标的 → 空状态 + 引导 | ✅ 已补 |
| `page-spec` §四 | 单标的 → **相关性区隐藏** | ✅ 已补 |
| `page-spec` §四 | 回测中 → 已有结果**不清空** | ✅ 已补 |
| `page-spec` §四 | 起点早于共同起始日 → 自动前移 + 提示 | ✅ 已有 |
| `page-spec` §四 | **起点晚于末端 → 阻止提交 + 提示** | ✅ 已补（按钮置灰 + 红字说明） |
| `page-spec` §四 | 无数据/代码无效 → 该行标红 | ✅ 已补（编辑器「状态」列 + `row-blocked` 整行标红） |
| `page-spec` §三-3 | **资产类型徽标**（详情表 + **曲线图例**） | ✅ 已补（详情表原有徽标；图例加「口径：后复权价 3 只 · 分红再投净值 1 只」） |
| `page-spec` §五 | 视觉：首格 **34px** · 表格行高 ≥48px · 数值右对齐 · 斑马纹 · **断点 1280/960** | ✅ 已补（主色/跌色沿用全局 `trend-up/down`，与规格的 `#E63946`/`#2A9D8F` 同族但取值不同 —— **全站统一优先于单页规格**） |
| `page-spec` §八 | 契约命名：`effective_start` ✅ **已新增**（2026-09-19 引擎 v6，与 `actual_start` 并存：前者=意图起点未对齐、后者=账本首个交易日）· `asset_class`（现 `security_type`）· `manager`（已改正）· `nav_series`（现 `nav.{dates,nav}`） | 🟡 部分完成：`effective_start`/`manager` 已对齐，其余待统一 |
| `page-spec` §八 | `metrics` 应含 `worst_year` / `turnover` / `recovery_days` | ✅ `worst_year`+`turnover` 已补（`recovery_days` 仍在 `drawdown` 里） |
| `page-spec` §9.6 | 新建/复制组合 → **立即**算三格 | ✅ 已补 |
| `page-spec` §9.6 | 成员/权重变更 → 重算 | ✅ 已补 |
| `ambiguity-audit` D5 | 指标卡**默认收起** | ✅ 已补（折叠 + 默认收起） |
| `ambiguity-audit` D8 | 相关性格子**显示样本数 n** | ✅ 已补（后端 `sample_sizes` → 格子小字 `n=2438`） |
| `ambiguity-audit` D9 | 数据太旧 → **拒绝添加** | ❌ 未实现（判定基准待定：末端 − 5 交易日） |
| `ambiguity-audit` D3 | 口径可比性**验证**(A) + 标注(B) | ⚠️ 只做了 B |
| `multi-portfolio` §六-1 | 标的库显示"**该标的被 N 个组合使用**" | ✅ 已补（`used_by`/`used_by_count` + 「被组合使用」列，归档组合不计） |
| `multi-portfolio` §0.2.4 | 端点 `POST /portfolios/{id}/duplicate` | ⚠️ 用 `POST /portfolios{from_id}` 实现，**形态不同、功能等价** |
| `add-asset-ux` §二第 3 步 | 确认卡含**类型描述 / 基金经理**，数据源出中文 | ✅ 已补（`fund_nav` 解析 `manager_name` → `confirmSubtitle`；`danjuan`→蛋卷） |
| `add-asset-ux` §二第 2 步 | 候选行显示**该候选的数据区间** | ⚠️ 部分（已抓取给真实区间；未抓取只回显「成立/最新 …（待抓取）」—— 不编造） |
| `add-asset-ux` §二第 4 步 · §四 | 编辑器**状态列**（同步中→就绪/失败）+ 失败**重试** | ✅ 已补（`rowStatusOf` 共用实现；重试只补该标的、不重跑回测） |
| `add-asset-ux` §一 | 编辑态可设**添加日** | ❌ 未实现（需 PATCH 契约支持 `added_at`） |

**结论**：本轮（21:30~22:20）已补 **10 项**（导航分组、空态入口、单标的隐藏相关性、失败不清空、缓存刷新时机 ×2、状态列+重试、行标红、被 N 个组合使用、确认卡经理/类型描述、图例口径构成、响应式断点）；
剩余 **4 项** 待做：
- ⚠️ **等你拍板**：`data-maintenance` §十⑦ 已裁决；**D9「数据太旧拒绝添加」的判定基准**（审计写"末端 − 5 交易日"）、**`[保存为模板]` 的语义**（复制组合 or 另建模板表）
- 🔧 可自行推进：**编辑态设「添加日」**（PATCH 成员项支持 `added_at`）、**契约命名对齐**（`effective_start` / `asset_class` / `nav_series`）

> ⚠️ **教训**：这份清单本应在 P3 收尾时就把每份文档逐条打勾产出，而不是等用户发现。
> 根因是"按自己的理解推进"而不是"按文档的清单逐项验收"——**以后每个阶段收尾必须做一次本文式的交叉核对**。
>
> ⚠️ **第二个教训（测试面）**：口径类模块此前**没有专门的纯函数测试**
> （`services/fund_nav.py` 的链式复权、`utils/portfolioAssets.mjs` 的起点/状态口径都只有间接覆盖）。
> 已补 `tests/test_fund_nav.py`（20 项）与 `src/utils/portfolioAssets.test.mjs`（9 项）——
> **凡是"算口径"的模块，必须有自己的纯函数测试**，组件测试覆盖不到它们。

---

## 五、未核对项（如实列出）

| 项 | 状态 | 原因 / 补法 |
| --- | --- | --- |
| ~~沪深300 基准 39.17%~~ | ✅ **已核对通过（2026-09-19）** | 腾讯 `sh000300` 分页取 2560 行（4 请求）：2016-09-14 收 3238.73 → 2026-09-18 收 4507.39 = **39.17%**，与页面完全一致。**副产品：证实韭圈儿基准是价格指数（不含股息）** |
| 季平衡残差 0.52pp | ⚠ 已定位未消除 | 折算每次调仓约 0.01pp，属调仓执行环节的微观差异；**不影响结论**，实现后按官方规则打「待校准」标记 |

---

## 六、与既有代码的关系

**复用（不新建）**

| 既有资产 | 用途 |
| --- | --- |
| `research_security` | **标的主表**（已含 `symbol`/`name`/`security_type`/`source`/`enabled`），即 `asset_master` 的八成；`upsert_securities()` **只增不删** |
| `run_research_daily_sync()` | **按 `enabled=True` 遍历**，用户新增标的次日自动纳入 17:30 定时同步，**零改动** |
| `research_trade_calendar` | 再平衡日期的权威依据（**不可用 `resample('M')`**） |
| `research_replay_run` | **Run 范式的参照物**（参数即身份 + `input_hash` + 算法版本 + 对比接口） |
| 腾讯 `fqkline` / 蛋卷 `djapi` | 股票·ETF / 场外基金 的序列来源（均已 ECS 验收） |

**新增**

| 表 | 说明 |
| --- | --- |
| `fund_nav_daily` | 场外基金复权净值（分红再投链式）→ ✅ **已建**，迁移 0006；端到端实测 100018：5545 行 / 2003-12-02 ~ 2026-09-18 / `unit_nav` 1.3684 → `adj_nav` 4.856 |
| `portfolio` | **组合身份**（`name`/`note`/`status`；**不含**再平衡/基准/区间） |
| `portfolio_asset` | 组合成员（**初始权重** + **`added_at`**），唯一键 `(portfolio_id, symbol)` |
| `backtest_run` | Run 元数据（**含 `rebalance`/`benchmark`/`start`/`end`** + `input_hash`） |
| `backtest_metrics` | Run 的绩效指标；**NAV 明细建议存文件**，DB 只存元数据（SQLite 膨胀问题） |

---

## 七、变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-19 | 创建本文档索引；补 `portfolio-lab-page-spec.md` §八 接口契约；标注 `portfolio-lab-flow.md` §五 作废；`history_start` 由"待定"升级为"已定：序列层不得依赖 `research_daily_bar_adjusted`" |
| 2026-09-19 | 模块验收完成：六类核对项 24 点全部 PASS，最大残差 0.52pp |
| 2026-09-19 | **红线项全关闭**：D2 定为**当日净值成交**（实测 T+1 偏 0.6~2.8pp；日历驱动不构成前视）；D4「添加后的收益」确认为**纯展示列、对回测零影响**，降级为非阻塞项 |
| 2026-09-19 | **起算日口径定稿（用户裁决）**：不引入"成立日/上市日"，一律以数据可得区间为准 → 取消 BASE 概念，回到 T0 单一起点；「成立来」与韭圈儿差 1.27pp，标注为口径差异并单列容差 ±1.5pp |
| 2026-09-19 | 🔴 **架构与产品 Review**（`portfolio-lab-arch-review.md`）：**发现一处验收与生产口径不一致** —— 513100 近10年 蛋卷净值 490.22% vs 腾讯场内价 578.53%（差 **+88.30pp**，主要为纳指ETF 溢价扩张 + 复权差异）。修正：**ETF/LOF/场外基金统一走蛋卷净值**，个股才走腾讯。另提 3 处可砍复杂度（删查看态/编辑态两态、回测异步→同步、前端不显示 `price_basis` 行话）与 5 处代码层建议（文档蒸馏/观测收敛/`.OF` 崩溃点排查/双口径说明/精度天花板） |
| 2026-09-19 | **新增数据维护设计**（`portfolio-lab-data-maintenance.md`）：复用既有 `tasks/registry` + `run_logger` + `data_catalog.Policy` 骨架；**三处结构性障碍**（`_assert_symbol_type` 绑交易所后缀 / `catalog()` source 硬编码 akshare 而实际 tencent / `history_start` 2021 太晚）；**两类数据形态**（股票ETF 双序列配对 vs 场外单序列 upsert）→ **两个 job**（`research_daily_sync` 扩用 + 新增 `fund_nav_sync`）；**三种触发**（定时 / 手动整批 / 手动单标的）；场外基金 4 个特殊处理（首日无 percentage / 链式复权以 `daily_return_pct` 为准 / 全量重拉查修订 / QDII 需 `trading_day_offset=1`） |
| 2026-09-19 | **文档自检**：8 份专题一致性审计，**修复 9 处**（BASE 概念残留 3 处、统计口径不一致 3 处、标题/状态过时 2 处、L1/L2 入口未区分 1 处）；回归脚本复跑 **6 类全 PASS**、无死链。新增 **§八 文档自检** |
| 2026-09-19 | **列表页交互闭合 + 补实现要点**：用户确认**点卡片本体进入**、**列表页有「复制组合」** → `✎`=就地改名、删除=软删+二次确认、空组合显示 `—`、指标**固定「不平衡」**；新增 `page-spec` §9.6：**列表指标必须缓存**（`cached_*` + `cached_asof_date`），⭐ 卡片上的「收益时间」即缓存的数据截止日 |
| 2026-09-19 | **组合列表页（L1）已按韭圈儿实拍截图对齐**（用户提供截图）：两列卡片网格、每卡三行（组合名+✎+删除 / 成立时间+收益时间 / 日收益·近一月·今年以来）、右上唯一按钮「创建组合」；**此前"表格+标的数+可用区间+回撤+排序/搜索"的设计全部作废**。见 `portfolio-lab-page-spec.md` §九 |
| 2026-09-19 | **组合范围定稿（用户三轮澄清）**：**支持多组合** —— L1 组合列表 + L2 组合详情；"平衡/不平衡/季/年"是**组合内部特性**（页面上一个切换，**落库归 `backtest_run`**）；⚠ 单组合不丢历史（`backtest_run` 存组合快照）。此前"退回单组合/不做列表页"的结论**已作废**（我误把"页面层级"读成"数量"，记于 `portfolio-lab-multi-portfolio.md` 0.2.5） |
| 2026-09-19 | ⚖ **裁决：场外基金单独用表存储，不实现 bar 协议**（用户 2026-09-19）。原文 §十⑦ 要求 `DanjuanProvider` 实现 `get_daily_bars`，与 §三「两类数据形态」自相矛盾。统一改放在**消费层序列层**（P2 的 `get_series()`，显式带 `price_basis = HFQ | NAV_ADJ`）。**代价如实记录**：`get_series()` 须分支读 `research_daily_bar_adjusted` / `fund_nav_daily` 两张表。`provider_factory("danjuan")` 未注册（已确认无路径调用） |
| 2026-09-19 | **P3 代码完成**（提交见下）：迁移 **0008** 建 `backtest_run`（组合快照 `members_json` + 参数 + 结果 `result_json`/`nav_json` + `input_hash` 幂等）· `services/backtest.py`（份额法账本/区间/回撤/相关性/指标/收益条七格）· `services/backtest_service.py`（编排 + Run 幂等落库 + 三格缓存刷新）· 4 个端点（`POST/GET /backtests`、`GET /backtests/{id}`、`GET /backtests/compare`）· `portfolio_cache_refresh` job（23:30，排在 `fund_nav_sync` 之后）· 前端 L2 八个展示区（收益条/Tabs/控制条/曲线/指标卡/相关性/详情表/口径提示）。**验收：`scripts/verify_engine_port.py` 对韭圈儿基准组合 6/6 PASS**；`test_backtest_engine.py` 33 项 + `test_backtest_service.py` 43 项 |
| 2026-09-19 | ⚠ **本轮实现中修掉的两个真 bug**（都不是风格问题）：① 默认区间"末端回推 10 年"会**早于建仓日 T0**，任何历史不足 10 年的组合直接回测失败 → 起点夹到 T0（与规格"起始日早于共同起始日自动前移"一致）；② 区间起点早于 T0 时 `window_return` 返回空 → 收益条大面积空白 → **退化为自 T0 起算**并由 `actual_start` 如实回显。另修前端 `Number(null) === 0` 导致"无相关性数据"被画成"零相关" |
| 2026-09-19 | **P2/P3 执行口径确认（用户答复）**：① 本机可直接真实抓取，但**不暴力测试对方服务器**（串行 + 间隔 + 退避，不做压测实验）；② 本地 `data/web.db` 为测试库，**可用 ECS 副本覆盖**，红线是**不在 ECS 生产上跑未验证/破坏性迁移**；③ P2 验收 = **先跑测试，能看页面最好** |
| 2026-09-19 | **P0 + P1 实现完成并验收**：`280a85b`（基础层）→ `bab81fe`（服务+API）→ `9e16f92`（前端标的库）→ `6fd2350`（场外基金链路）。P1 端到端实测（100018，**1 次请求**）：**5545 行** / 2003-12-02 ~ 2026-09-18 / 成立首日 `percentage` 为 None 已兜底 / 链式复权 `unit_nav` 1.3684 → `adj_nav` 4.856（分红再投累计口径）。验收脚本 `scripts/verify_p1_fund_nav.py`。**并修一处实现偏差**：场外基金 `Policy` 补 `trading_day_offset=1`（原缺失 → 数据管理页每个交易日都会误报 `lagging` 红色告警）。**下一阶段 = P2 统一序列层 + 组合定义** |

---

## 八、文档自检（2026-09-19 复核）

对 8 份专题文档做了一次一致性审计，**共发现并修复 9 处**：

| # | 问题 | 修法 |
| --- | --- | --- |
| 1 | `algorithm.md` 仍写"持仓路径从**建仓基准日**（2013-04-25）累计" | 改为 **T0（2013-04-26）** —— BASE 概念已在用户裁决后取消 |
| 2 | `page-spec.md` §区域① 仍写"从**建仓基准日**等权建仓" | 同上 |
| 3 | `ambiguity-audit.md` §一 仍写 `n_i = w_i / P_i(BASE)` | 改为 `P_i(T0)` |
| 4 | `ambiguity-audit.md` §〇 的"3 红线 + 8 产品取舍"与统计不一致，且 D2/D4 行脱离表格 | §〇 整节重写 |
| 5 | 统计口径三处不一致（27/9、23、11） | 统一为 **明确 28 · 仍有歧义 7 · 待核实 1**，并加口径说明 |
| 6 | `multi-portfolio.md` §七 标题"7 件事"但表有 11 行 | 改为"决策清单（11 项）" |
| 7 | `multi-portfolio.md` §八"需要同步修改的文档"是未来时，实际已完成 | 改为「§九 本文档引出的后续（状态）」，全部标 ✅ |
| 8 | `add-asset-ux.md` 未区分 L1 列表页入口与 L2 详情页入口 | 补一段"勿混"说明 |
| 9 | `add-asset-ux.md` §六 的 6 项无状态标记 | 标注 ✅已采纳 3 项 / ⬜仍待确认 3 项，并指向索引 §四 |

**复核通过项**：

- 回归脚本复跑：**A~F 六类全部 PASS**
- 全部文档**无死链**（`docs/*`、`scripts/*` 引用均存在）
- 口径速查 16 条与各专题文档**无冲突**
- 已废弃概念（`BASE`、`Σwᵢrᵢ` 加权、三 Run 成本分解、成本分档、T+1 成交、单组合）**均已带作废/更正标记**

**已知的历史遗留（不影响当前结论）**：

- `portfolio-lab-multi-portfolio.md` 保留 **0.2.5 作废记录**（我前两轮误读"单/多"为数量而非层级）—— **故意保留**，避免重犯
- `portfolio-backtest-engine-design.md`、`superpowers/specs/2026-09-18-next-day-t-data-and-safety-design.md` 含**部分过时内容**，均已在文首加更新块/核对补注
